# Heart Disease Prediction (Shiny Application)

This project is an interactive R Shiny application that predicts the probability of heart disease using a trained Random Forest model. The UI is styled with a transparent glass card, a split-page layout, and a separate result panel for clarity.

---

##  Features

- Clean 3-column input layout
- Medical + lifestyle attributes
- Random Forest–based prediction model
- Probability output (0–100%)
- Professional UI with background image
- Separate result page with overlay card
- Completely reproducible in R/Shiny

---

## 📁 Project Structure

